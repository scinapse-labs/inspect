[workspace]
members = ["crates/inspect-core", "crates/inspect-cli", "crates/inspect-mcp", "crates/inspect-api"]
resolver = "2"

[profile.release]
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"
